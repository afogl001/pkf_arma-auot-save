#!/bin/bash

#echo "What save would you like to load?"
#read vSaveNum 
#find ~/ -name save.fps.* -type f -print | grep ARMA | sort -r | head -$vSaveNum | tail -1
#cp -f $vSavePath/$(ls $vSavePath | sort -r | head -1) $vSavePath/continue.fps


#echo "EUID = $EUID"
#echo "UID = $UID"
echo $EUID
echo $HOME
